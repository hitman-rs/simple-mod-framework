# behavior.txt

A HITMAN behaviour tree in textual format, as extracted by GlacierKit.

This will overwrite any existing resource, so using this (or a raw AIBZ file) to overwrite any of the game's few existing behaviour trees will make your mod incompatible with all others overwriting the same file.
