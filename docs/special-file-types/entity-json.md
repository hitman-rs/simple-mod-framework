# entity.json

An entity template (TEMP/TBLU) in QuickEntity format, as extracted by GlacierKit.

The framework will overwrite any existing resources with the given data.

If the `subType` of the entity is set to `scene` or `brick`, and a path is supplied for the factory ID, the entity will automatically be added to packagedefinition, making it available for use in contracts.
