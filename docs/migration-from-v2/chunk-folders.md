# Chunk folders

SMFv2 content folders contained chunk folders within them, named the same as the game's RPKG files (e.g. `chunk0`). SMFv3 now names these by the name of the partition which produces those files; the `chunk0` folder, for example, is now `super`.
