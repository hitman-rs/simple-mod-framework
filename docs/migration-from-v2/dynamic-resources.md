# Dynamic resources

Some mods previously used the `thumbs` key to disable dynamic resources, so that repository mods would work in online mode. This key has been removed; the Simple Mod Framework now automatically disables dynamic resources whenever the repository is modified (though this behaviour is controllable by the user).
